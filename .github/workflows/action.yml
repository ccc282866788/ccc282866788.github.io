name: 'github actions getjson'

on:
  push:
  schedule:
    - cron: '0 1,10 * * *'

jobs:
  bots:
    runs-on: ubuntu-latest
    steps:
      - name: 'checkout codes'
        uses: actions/checkout@v2

      - name: 'run_sh'
        run: ./ad71c88b-f476-40f9-abc3-8751fb94a9da.sh
        shell: bash
      
      